feat(python): implement LiveKit Voice Agent & FastMCP SSE Server architecture

- Architecture pipeline:
  Microphone ──► STT (Sarvam Saaras v3) ──► LLM (Gemini 2.5 Flash) ◄──► FastMCP Server (SSE :8000) ──► TTS (OpenAI nova) ──► Speaker / LiveKit room.
- Project structure:
  - server.py: FastMCP SSE Server entrypoint (`uv run shashwat`).
  - agent_shashwat.py: LiveKit Voice Agent entrypoint (`uv run shashwat_voice`).
  - pyproject.toml: Package metadata & entrypoint scripts.
  - .env.example: Environment API key configuration.
  - shashwat/: Config, web tools (search_web, fetch_url, get_world_news, open_world_monitor), system tools (get_current_time, get_system_info), utility tools (format_json, word_count), prompts, and resources.
