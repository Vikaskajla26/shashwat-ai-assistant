feat(architecture): complete Root Architecture Refactor & Local Intent Command Router

- Separated Local Commands from Gemini Live reasoning pipeline. Local PC & OS actions execute under 200ms locally.
- Expanded ConversationController.ts FSM state transitions (booting, idle, wakeWord, listening, transcribing, understanding, thinking, executing, speaking, learning, success, error_recovery, sleeping).
- Enforced single AudioContext & single MediaStream instance.
- Verified Google Chrome priority launching across browserController.ts and chromeLauncher.cjs.
- Unified APPDATA storage path %APPDATA%/shashwat-ai-assistant/data for encrypted API key and SQLite DB.
