fix: resolve activeAudioVolume ReferenceError & add safe null-checking defaults

- Located and fixed missing activeAudioVolume declaration in ShashwatDittoDashboard.tsx.
- Destructured inputVolume and outputVolume from props and computed activeAudioVolume with safe fallbacks:
  const activeAudioVolume = volumeRef?.current ?? (state === 'speaking' ? (outputVolume ?? 0) : (inputVolume ?? 0));
- Added strict null-checking & fallback defaulting to QuantumAICore.tsx and AudioWaveform.tsx so visuals default to 0 if microphone data is absent.
- Verified build and app rendering with zero runtime ReferenceErrors.
