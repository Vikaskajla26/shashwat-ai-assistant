perf(ui): optimize rendering to 120 FPS and clean up unused scratch files

- Throttled audio volume updates in App.tsx to eliminate 60 FPS React component re-render thrashing.
- Wrapped Orb in React.memo and added GPU hardware acceleration hints (will-change: transform, transform: translateZ(0)) to Orb.tsx canvas.
- Deleted unused test script test_browser_render.js.
