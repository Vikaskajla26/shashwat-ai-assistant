fix(electron): resolve localhost connection refused and silent exe exit

1. Added HTTP Health Check Gate (waitForServerReady) in electron/main.cjs:
   - Electron main process now polls http://127.0.0.1:3000/api/health until Express server is live & ready before invoking loadURL('http://localhost:3000').
   - Eliminates premature connection attempts and fixes 'localhost refused to connect' errors.

2. Fixed Silent Executable Quit on Launch:
   - Added orphaned process taskkill handling before single instance lock evaluation so launching Shashwat-AI-Assistant.exe always opens the UI cleanly.

3. Updated WebSocket and API Protocol Fallbacks:
   - Added rawHost fallback ('localhost:3000') in src/modules/LiveSession.ts when window.location.host is empty.
   - Fixed relative fetch path resolution in src/App.tsx under file protocol fallbacks.
