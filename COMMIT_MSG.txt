fix(persistence): unify data directory resolution to %APPDATA%/shashwat-ai-assistant/data

- Replaced ad-hoc `path.join(process.cwd(), 'data')` computations with shared `getDataDir()` from server/utils/paths.ts in:
  1. server/providers/providerStorage.ts (ai_providers.enc)
  2. server/utils/persistenceManager.ts (app_state.json)
  3. server/db/database.ts (shashwat.db SQLite database)
  4. server/tools/memory.ts (memory.json)
  5. server/tools/aiWorkspace.ts (ai_workspace_state.json)
- Preserved server/tools/nircmd.ts BIN_DIR relative to install folder as intended.
- Guarantees persistent storage across restarts, app updates, and production Electron process.chdir() execution.
