feat(phase-3): implement 3-Tier Human Memory System & state auto-resume

- Rebuilt MemoryManager.ts implementing 3-tier memory model:
  * Tier 1: Short-Term Working Memory (RAM context, active goals, transient parameters).
  * Tier 2: Conversation Memory (Time-stamped dialogue turns & speaker verification tags).
  * Tier 3: Encrypted Long-Term Memory (AES-256-GCM encrypted SQLite storage for User, Preferences, Projects, Tasks, Browser History).
- Implemented live Memory Editing, Deletion, and JSON/Markdown Export.
- Implemented environment state auto-resume on application restart.
- Added IPC handlers in ipcHandlers.cjs for db:export-memories, db:save-resume-state, db:get-resume-state.
- Created Phase3MemorySelfTest.ts verification suite ensuring 100% test pass score.
